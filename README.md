# Prompt Template Backend

This project provides a simple Express-based backend for storing prompt templates.
Templates are persisted in `templates.json` in the project root.

## Usage

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the server:
   ```bash
   npm start
   ```

The server exposes CRUD endpoints under `/templates`.

